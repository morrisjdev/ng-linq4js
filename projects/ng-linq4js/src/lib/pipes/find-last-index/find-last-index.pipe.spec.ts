import { FindLastIndexPipe } from './find-last-index.pipe';

describe('FindLastIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new FindLastIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
