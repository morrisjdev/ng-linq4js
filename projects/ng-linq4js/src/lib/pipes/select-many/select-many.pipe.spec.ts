import { SelectManyPipe } from './select-many.pipe';

describe('SelectManyPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectManyPipe();
    expect(pipe).toBeTruthy();
  });
});
