import { ThenByDescendingPipe } from './then-by-descending.pipe';

describe('ThenByDescendingPipe', () => {
  it('create an instance', () => {
    const pipe = new ThenByDescendingPipe();
    expect(pipe).toBeTruthy();
  });
});
