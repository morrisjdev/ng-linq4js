import { FindIndexPipe } from './find-index.pipe';

describe('FindIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new FindIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
